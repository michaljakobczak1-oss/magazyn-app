services:
  magazyn:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=zmien-mnie-na-losowy-ciag
    volumes:
      - ./data:/app/data
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped
